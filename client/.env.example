# Optional: use only if the API runs on a different host/port (e.g. colleague's machine or deployed backend).
# Leave unset to use the proxy (default: requests go to http://localhost:3000 when using npm run dev from root).
# REACT_APP_API_URL=http://localhost:3000
# REACT_APP_API_URL=http://192.168.1.10:3000
